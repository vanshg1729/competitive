/* https://codeforces.com/contest/1634/problem/B */
#include <bits/stdc++.h>
using namespace std;
#define sc second
#define fr first
#define ll long long
const int maxn = 2e5 + 10;

int main() {

	//freopen("input.txt", "r", stdin);
	//freopen("output.txt", "w", stdout); 
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	
	int T; cin >> T;
	while (T--) {
		ll n, x, y, res; cin >> n >> x >> res;
		y = x + 3;
		ll arr[n + 10] = {};
		for (int i = 1; i <= n; i++) cin >> arr[i];
			

}
